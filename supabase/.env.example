# Server-only. Copy to supabase/.env; never commit actual values.
TELEGRAM_BOT_TOKEN=
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are supplied by the Edge runtime.
# Production ALLOWED_ORIGINS=https://zayneev.github.io (origin has no path).
